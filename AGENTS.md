# AI Vision RT1064 Agent Notes

## Agent skills

### Issue tracker

Issues and PRDs are tracked as local Markdown files under `.scratch/<feature-slug>/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Triage uses the default `mattpocock/skills` role names as local Markdown status strings. See `docs/agents/triage-labels.md`.

### Domain docs

This is a single-context repo: read `CONTEXT.md` at the repo root and ADRs under `docs/adr/` when present. See `docs/agents/domain.md`.

